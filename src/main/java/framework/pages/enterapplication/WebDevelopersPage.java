package framework.pages.enterapplication;

import org.openqa.selenium.WebDriver;

import framework.pages.Page;

public class WebDevelopersPage extends Page{

	public WebDevelopersPage(WebDriver webDriver) {
		  super(webDriver);
		 }
}
